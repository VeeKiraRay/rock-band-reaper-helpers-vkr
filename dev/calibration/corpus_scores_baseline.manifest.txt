Difficulty calibration run manifest
(generated by run_calibration_vkr.lua - do not hand-edit)

date            : 2026-08-17 08:14
scorer          : span-aware segments + authored markers + held notes + entropy rate + attack-based density + drum limb split + per-instrument gem window and markers (pro keys lane shifts) + vocal scorer (pitch-class intervals, tick-matched lyrics)
corpus songs    : 526  (greenday=5, lego=45, rb2=79, rb3_dlc=397)
corpus root 1   : C:\Users\wejaa\_git_rb\rock-band-reaper-helpers-vkr\_external_docs/reference_songs/
corpus root 2   : C:\Users\wejaa\_git_rb\rock-band-reaper-helpers-vkr\_external_docs/new_reference_songs/
corpus root 3   : C:\Users\wejaa\_git_rb\rock-band-reaper-helpers-vkr\_external_docs/hard_reference_songs/
rows this run   : 2101
rows skipped    : 0  (already present)
missing MIDI    : 132
warnings        : 0
instruments     : guitar, bass, drum, keys, real_keys, vocals
factor columns  : 96

Column order is the CSV header itself, which is the authoritative
record of the factor set - a differing header means a different scorer.

----------------------------------------------------------------------
VERDICT (hand-written, appended after the generated block above)
----------------------------------------------------------------------

This is the 96-column factor set as it stood after round 18, and the run the
six shipped models in lib/reaper_difficulty_models.lua were exported from
(helper v0.9.57). It is kept as the baseline so run_calibration_diff_vkr.lua
has something to compare the rounds 19-22 rescore against.

Selected by the locked protocol, with the release gate read from the
pessimistic end of each interval (floors: usable 90%, miss 5%, rho 0.70):

  guitar     full@attacks / log(rank)                  21 feat   91.64%  +0.862  PASS
  bass       baseline+entropy / log(rank)               3 feat   91.68%  +0.802  PASS
  drum       full_drum / log(rank)                     26 feat   93.38%  +0.888  PASS
  keys       primary+ent_rel+complex@attacks-chord      7 feat   89.94%  +0.878  fail
             / rank
  real_keys  primary+ent_rel@attacks / rank             7 feat   85.89%  +0.861  fail
  vocals     parts+tess+move / log(rank)               12 feat   85.12%  +0.668  fail

Three instruments fail the usable floor. Keys misses it by 0.06 points, which
two separate collection attempts failed to close (the added charts raised n and
lowered the mean by almost exactly the cancelling amount). Vocals fails both the
usable floor and the rho floor and is the instrument rounds 19-21 target.

WHAT THE NEXT RUN CHANGES, and what the diff must therefore show. Eighteen new
columns across four declared rounds:

  round 19  phrase_span_{mean,p90}, phrase_travel_{mean,p90}          vocals, 4
  round 20  parts_2, parts_3, parts_log                               vocals, 3
  round 21  breath_{max,mean,move,movemax}_{50,100}                   vocals, 8
  round 22  {density,attack_density,hand_density}_peak_noroll          drums, 3

NONE of them redefines an existing column. Round 22 in particular was written as
new twins rather than a fix to density_peak / attack_density_peak /
hand_density_peak precisely so this stays true - those three are fitted in five
shipped models, and changing them in place would re-open every instrument at
once and drown any real regression in the diff.

So the diff check is unambiguous: every one of the 96 columns below must read
0% moved. The 18 new ones will report as absent from one of the two runs, which
is expected. Any movement in an old column means a measurement changed by
accident and the rescore must be investigated before any model is refitted.
